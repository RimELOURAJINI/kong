# Kong Project

Ce projet contient une stack Docker avec :

- Kong (API Gateway)
- Konga (interface graphique)
- PostgreSQL (base pour Konga)

## Comment démarrer

1. Installer Docker et Docker Compose.
2. Dans le dossier `kongProject`, lancer :

```bash
docker-compose up -d
